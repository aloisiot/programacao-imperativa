// Verifica a existência de determinado elemento no array
// Retorna um valor boleano
// Parâmetro:
//      elemento: valor a ser procurado.
//      inicio: índice ao qual se inicia a busca.

let nomes = ['Maria', 'Ana', 'José', 'Davi'];

console.log(nomes.includes('Ana')); // true
console.log(nomes.includes('Marcos')); // false