// Percorra o array abaixo e exiba seus elementos:

const pontos = [12, 34, 56, 34, 5, 8, 76, 4, 1, 90];

// Imprime todos os elementos de um array.
function printElements(array){
    for(let i = 0; i < array.length ;i++) {
        console.log(array[i]);
    }
}

printElements(pontos);