// Percorrendo um array e modificando cada um dos elementos

let nomes = ['Alana',
            'Diego',
            'Silvia',
            'Alice'];

for(i = 0; i < nomes.length; i++) {
    nomes[i] = 'Elemento alterado';
}

console.log(nomes);